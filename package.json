{
  "scripts": {
    "test": "npm run build && node dist/main",
    "build": "echo 开始清理旧文件 && del dist /s /q && echo 开始编译 && tsc --declaration && echo 编译完成"
  },
  "dependencies": {
    "events": "^3.3.0",
    "websocket": "^1.0.34"
  },
  "devDependencies": {
    "@types/events": "^3.0.0",
    "@types/websocket": "^1.0.5"
  }
}
